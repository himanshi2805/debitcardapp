/**
 * @format
 */

import {AppRegistry} from 'react-native';
import DebitCardApp from './src/App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => DebitCardApp);
